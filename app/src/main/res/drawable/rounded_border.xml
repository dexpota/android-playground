<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/holo_blue_dark" />
    <stroke android:width="10dp" android:color="@android:color/holo_blue_light" />
    <corners android:radius="20dp" />
    <!-- This padding is needed if you want your content not to overlap with the border -->
    <padding
        android:bottom="15dp"
        android:left="15dp"
        android:right="15dp"
        android:top="15dp" />
</shape>